version: "3.1"

migrations:
  dir: ./migrations
  table: goose_db_version
  dialect: postgres

db:
  host: ${DB_HOST}
  port: ${DB_PORT}
  database: ${DB_NAME}
  user: ${DB_USER}
  password: ${DB_PASSWORD}
  sslmode: ${DB_SSLMODE}
